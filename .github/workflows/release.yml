name: Release

on:
  push:
    tags:
      - 'v*'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  release:
    name: Build & Push
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - uses: actions/checkout@v5

      - name: Compute bare version
        run: echo "VERSION_BARE=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ env.VERSION_BARE }}
            COMMIT=${{ github.sha }}
            BUILD_TIME=${{ github.event.head_commit.timestamp }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: Build frontend
        working-directory: web
        run: npm ci && npm run build

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Install tools
        run: |
          go install github.com/swaggo/swag/cmd/swag@latest
          go install github.com/josephspurrier/goversioninfo/cmd/goversioninfo@latest

      - name: Generate Swagger docs
        run: swag init -g cmd/server/main.go

      - name: Generate Windows PE resource
        run: |
          VER_MAJOR=$(echo "${{ env.VERSION_BARE }}" | cut -d. -f1 | grep -oE '^[0-9]+' || echo 0)
          VER_MINOR=$(echo "${{ env.VERSION_BARE }}" | cut -d. -f2 | grep -oE '^[0-9]+' || echo 0)
          VER_PATCH=$(echo "${{ env.VERSION_BARE }}" | cut -d. -f3 | grep -oE '^[0-9]+' || echo 0)
          goversioninfo \
            -file-version="${{ env.VERSION_BARE }}" \
            -product-version="${{ env.VERSION_BARE }}" \
            -ver-major="${VER_MAJOR}" \
            -ver-minor="${VER_MINOR}" \
            -ver-patch="${VER_PATCH}" \
            -o cmd/server/resource_windows_amd64.syso \
            cmd/server/versioninfo.json

      - name: Build binaries
        run: |
          LDFLAGS="-s -w \
            -X github.com/tlsentinel/tlsentinel-server/internal/version.Version=${{ env.VERSION_BARE }} \
            -X github.com/tlsentinel/tlsentinel-server/internal/version.Commit=${{ github.sha }} \
            -X github.com/tlsentinel/tlsentinel-server/internal/version.BuildTime=${{ github.event.head_commit.timestamp }}"

          mkdir -p dist
          CGO_ENABLED=0 GOOS=linux   GOARCH=amd64 go build -ldflags="$LDFLAGS" -o dist/tlsentinel-server-linux-amd64       ./cmd/server
          CGO_ENABLED=0 GOOS=linux   GOARCH=arm64 go build -ldflags="$LDFLAGS" -o dist/tlsentinel-server-linux-arm64       ./cmd/server
          CGO_ENABLED=0 GOOS=darwin  GOARCH=arm64 go build -ldflags="$LDFLAGS" -o dist/tlsentinel-server-darwin-arm64      ./cmd/server
          CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build -ldflags="$LDFLAGS" -o dist/tlsentinel-server-windows-amd64.exe ./cmd/server

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: dist/*
